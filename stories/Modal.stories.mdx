import { useState } from 'react';
import { Meta, Story } from '@storybook/addon-docs';
import { Modal } from '../example/Modal/Modal';
import { MultiSelect } from '../example/MultiSelect/MultiSelect';
import { items } from './items';

<Meta
  title="Examples/Modal"
  component={Modal}
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

# Modal

export const Template = (args) => {
  const [value, setValue] = useState();
  const onSelect = (value) => {
    setValue(value);
  };
  return (
    <Modal>
      <MultiSelect {...args} value={value} onSelect={onSelect} />
    </Modal>
  );
};

<Story
  name="Modal"
  args={{
    items,
  }}
>
  {Template.bind({})}
</Story>

[code](https://github.com/RedMadRobot/use-dropdown/tree/master/example/Modal/Modal.tsx)
